// .sequelizerc

const path = require("path");

module.exports = {
  config: path.resolve("config", "config.js"),
  "models-path": path.resolve("custom", "models"),
  "seeders-path": path.resolve("custom", "seeders"),
  "migrations-path": path.resolve("custom", "migrations"),
};
